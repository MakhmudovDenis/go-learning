package main  
  
import (  
    "fmt"  
)  
  
func main() {  
    fmt.Println("start") 
    for i := 1; i < 4; i++ {  
       defer fmt.Println(i) // вызовы defer кладутся в стек, из которого потом вытаскиваются в обратном порядке 
    }  
    fmt.Println("end")  
}

Вывод:
start
end
3
2
1
